"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WebsocketInterceptorService = void 0;
const common_1 = require("@nestjs/common");
const abstract_interceptor_service_1 = require("./abstract-interceptor.service");
let WebsocketInterceptorService = exports.WebsocketInterceptorService = class WebsocketInterceptorService extends abstract_interceptor_service_1.AbstractInterceptorService {
    getCallPoint(context) {
        return context.switchToWs().getPattern();
    }
    /**
     * A helper method to get the websocket client from the Nest Execution Context
     * @param context execution context from Nest
     * @returns the client object for the websocket adapter
     */
    getClient(context) {
        return context.switchToWs().getClient();
    }
    setRequestId(context, requestId) {
        const client = this.getClient(context);
        client.requestId = requestId;
    }
    getRequestId(context) {
        return this.getClient(context).requestId;
    }
};
exports.WebsocketInterceptorService = WebsocketInterceptorService = __decorate([
    (0, common_1.Injectable)()
], WebsocketInterceptorService);
