import { ArgumentsHost } from '@nestjs/common';
import { DelegatorService } from './interceptor/providers';
import { OgmaService } from './ogma.service';
export declare class OgmaFilterService {
    private readonly service;
    private readonly delegator;
    private json;
    private color;
    constructor(service: OgmaService, delegator: DelegatorService);
    log(exception: Error, host: ArgumentsHost): void;
    private doLog;
    private hasAlreadyBeenLogged;
}
